name: Get HACS
version: "1.3.1"
slug: get
image: "ghcr.io/javishome/{arch}-addon-get"
description: The easiest way to get HACS for Home Assistant
startup: once
boot: manual
url: "https://github.com/javishome/get_hacs/tree/main/get"
arch:
  - aarch64
init: false
schema:
  channel: list(current|development)?
map:
  - homeassistant_config:rw
